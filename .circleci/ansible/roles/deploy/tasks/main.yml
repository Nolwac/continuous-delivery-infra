- name: extract artifact
  become: yes
  unarchive:
    src: ~/project/artifact.tar.gz
    dest: /
    remote_src: no

- name: Install dependencies and start server
  become: yes
  command:
    cmd: npm install && pm2 stop default && pm2 start npm -- start
